# realtouch

A soft, tactile button demo for the web that reacts to pointer position, touch pressure, and click duration to feel closer to a real physical button.

## Run locally

Open `/home/runner/work/realtouch/realtouch/index.html` in a browser, or serve the repository root with a simple static server such as:

```bash
python3 -m http.server 8000
```
